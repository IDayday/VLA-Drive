# Execution-only choices; a new measured layout is required. Original YAML remains replayable.
inherits: planreg_wm_v2_base.yaml
read_only_attention_backend: split_sdpa
overlap_metric_target_with_ema: true
gradient_checkpointing: false
