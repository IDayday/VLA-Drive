# Phase-6 artifacts

Aggregate CSV/JSON files, per-run confidence intervals, the Gate-3 decision,
failure examples, and the core comparison plot are versioned here. The larger
`*_per_scene.jsonl` intermediates remain in the local output and are ignored;
they are deterministically regenerated by the evaluation command documented in
`docs/research/action_effect_world_model/reproduction.md`.
